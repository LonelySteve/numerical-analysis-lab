%% macro include_by_name(names)
%% for name in names
\noindent
\textit{\pyvar{name + ".m"}}
\begin{lstlisting}[frame=single]
\pyblock{set rel_path = name+".m"}
\pyvar{rel_path | include_code}
\end{lstlisting}
%% endfor
%% endmacro

\subsubsection{问题一}
\pyvar{include_by_name(["gauss"])}
\subsubsection{问题二}
\pyvar{include_by_name(["cholesky", "squareRoot"])}
\subsubsection{问题三}
\pyvar{include_by_name(["catchup"])}
